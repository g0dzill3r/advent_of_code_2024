package week3.day17

import util.withInput

fun main () {
    println("day$DAY, part1 ${if (SAMPLE) "(SAMPLE)" else "}"}")
    withInput(DAY, SAMPLE) { input ->
        println (input)
    }

    return
}

// EOF

